name: Kubernetes API Checker
on: [push, pull_request, workflow_dispatch]
jobs:
  k8s_api_checker:
    uses: Cray-HPE/hms-build-chart-workflows/.github/workflows/k8s_api_checker.yaml@sort-k8s-api-checker-casmhms-6190
    secrets: inherit
